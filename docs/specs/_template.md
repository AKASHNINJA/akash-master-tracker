# Feature: [title]

**Slug**: `[kebab-case]`  
**Issue**: #[n] (optional)

## Problem

## User stories

- As a …, I want …, so that …

## Acceptance criteria

- [ ]

## Non-goals

## Implementation notes (Akash Master Tracker)

- Touch only `index.html`, `app.js`, `style.css` unless there is a strong reason to add assets.
- Follow IIFE `TRACKERS.*` pattern; `localStorage` keys **`amt_`** prefix.
- Mobile-first (~430px); use CSS variables (`--accent`, `--surface`, …).
