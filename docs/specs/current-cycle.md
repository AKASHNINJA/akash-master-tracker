# Current cycle plan

This file is **overwritten** by the autonomous **`../tracker-agents`** orchestrator each cycle when you run `npm start` there.

When working only in **Cursor**, replace this with the active feature spec or point to another file under `docs/specs/`.

_No plan written yet — PM agent should create the first spec here or in a named `docs/specs/<slug>.md`._
